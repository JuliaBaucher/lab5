{
  "name": "julia-cv-embedding-builder",
  "version": "1.0.0",
  "description": "Lambda function to generate embeddings for Julia's CV knowledge base",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "embeddings",
    "openai",
    "rag"
  ],
  "author": "Julia Baucher",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "openai": "^4.67.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}